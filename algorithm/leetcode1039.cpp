class Solution {
public:
    int minScoreTriangulation(vector<int>& A) {
    	int n = A.size(), len, i, j, k;
    	vector<vector<int>> dp(n, vector<int>(n, 1));
    	for(len = 3; len < n; ++len)
    	{
    		for(i = 0; i < n; ++i)
    		{
    			
    		}
    	}
    }
};