class Solution {
public:
    vector<int> avoidFlood(vector<int>& rains) {
    	vector<int> ans(rains.size(), -1);
    	unordered_map<int,int> m;//湖泊数字，下雨天数
    	for(int i = 0; i < rains.size(); ++i)
    	{
    		if
    	}
    }
};