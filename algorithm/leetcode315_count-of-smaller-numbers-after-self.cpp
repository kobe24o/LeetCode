class Solution {
	struct TreeNode
	{
		int val;
		int count;
		TreeNode *left, *right;
		TreeNode(int v): val(v), count(0) {}
	};
public:
    vector<int> countSmaller(vector<int>& nums) {
        vector<int> ans(nums.size());
        
    }
};