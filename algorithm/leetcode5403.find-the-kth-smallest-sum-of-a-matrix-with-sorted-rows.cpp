class Solution {
public:
    int kthSmallest(vector<vector<int>>& mat, int k) {
    	vector<int> ans(mat[0]);
    	int i, j, ki;
    	for(i = 1; i < mat.size(); ++i)
    	{
    		multiset<int> s;
    		for()
    	}
    }
};