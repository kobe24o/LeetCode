class Solution {
public:
    int minTimeToVisitAllPoints(vector<vector<int>>& points) {
    	int i, dx, dy, prex = points[0][0], prey = points[0][1];
        for(i = 1; i < points.size(); ++i)
        {
        	
        }
    }
};